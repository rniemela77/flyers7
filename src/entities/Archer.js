import Unit from './Unit.js';

class Archer extends Unit {
  constructor(scene, team, x, y) {
    super(scene, team, 'archer', x, y);
  }

  // Add any specific methods or overrides for Archer here
}

export default Archer; 
