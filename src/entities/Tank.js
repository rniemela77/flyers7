import Unit from './Unit.js';

class Tank extends Unit {
  constructor(scene, team, x, y) {
    super(scene, team, 'tank', x, y);
  }

  // Add any specific methods or overrides for Tank here
}

export default Tank; 
