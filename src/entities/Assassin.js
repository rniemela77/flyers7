import Unit from './Unit.js';

class Assassin extends Unit {
  constructor(scene, team, x, y) {
    super(scene, team, 'assassin', x, y);
  }

  // Add any specific methods or overrides for Assassin here
}

export default Assassin; 
